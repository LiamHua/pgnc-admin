spring:
  application:
    name: user-manage
  cloud:
    nacos:
      discovery:
        server-addr: huazai.fun:8848
      config:
        server-addr: huazai.fun:8848
        file-extension: yml
        shared-configs: mysql.yml, common.yml
        refresh-enabled: true
        group: pgnc-admin