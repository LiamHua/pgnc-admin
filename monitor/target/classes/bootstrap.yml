spring:
  application:
    name: admin-server
  cloud:
    nacos:
      discovery:
        server-addr: huazai.fun:8848
      config:
        server-addr: huazai.fun:8848
        file-extension: yml
        shared-configs: common.yml
        refresh-enabled: true
        group: pgnc-admin